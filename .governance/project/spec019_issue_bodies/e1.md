Spec: SPEC-019 **R19-13**. Parent: Epic E. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Need a single evidence doc that the program actually fixed the live-run failures.

## Do

Write `.docs/docs-for-cli-tools/SPEC019_E1_E2E_SMOKE.md` covering:

- Two-host Nmap → Nerva non-empty fingerprints (count matches real ports, not cartesian)
- Nerva graph hydrated from `--output` (not empty SUCCESS)
- Nuclei `i/n` is batches; `crawl_urls` has no DOMAIN_NAME hostnames
- YAML: HTTPX/Katana absent from collector.dependencies; Nmap/Nerva semantic-export present
- Subfinder: COMPANY → DOMAIN_NAME → SUBDOMAIN
- HTTPX/Katana URLs hang off website roots

Use automated tests where they exist; live Run is optional supporting evidence, not required if fixtures cover the matrix.

## Files

- `.docs/docs-for-cli-tools/SPEC019_E1_E2E_SMOKE.md` (create)
- Index E1 → done

## Forbidden

Claiming done without the matrix; re-opening product scope.

Branch from `develop`; PR into `develop`.
