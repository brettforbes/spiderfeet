Spec: SPEC-019 **R19-06**. Parent: Epic B. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Need proof that a `--list` line file of `ip:port` reaches Nerva argv and, after A3, is non-cartesian.

## Do

1. Fixture/dry-run: `--list` file with `ip:port` lines → fingerprint `records[]` (or adapter graph nodes).
2. Regression: empty `ip_port_list` still `empty: skip_step`.
3. Cartesian regression is **A3’s** synthetic graph; this issue consumes that list shape, does not re-implement GSE.

## Files

- `.seed/12A_Workflow_YAML_Example.yaml` (argv `--list` stays)
- `spiderfeet_v2/workflow/tests/` (argv receives list file)
- Nerva adapter tests

## Acceptance

- Non-empty list file → Nerva invoked with `--list`; records non-empty on a fixture with JSONL.
- Empty list → skip_step.
- Index B2 → done.

## Forbidden

Nerva-only GSE dialect; `-t` comma list as the 12A contract.

Branch from `develop`; PR into `develop`.
