Spec: SPEC-019 **R19-16**. Parent: Epic F. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Adapters will each invent COMPANY/DOMAIN wiring unless a shared helper exists. Needs A1 parent cache.

## Do

Both `topology.py` copies: `add_company_domain_tree(builder, scan_id, apex, company_name=None)`:

- SCAN_RECORD `--contains-->` COMPANY
- Unknown name: `nugget_data = company:{apex}` (no COMPANY_NAME child)
- Known name: COMPANY `--had-->` COMPANY_NAME (descriptor)
- COMPANY `--contains-->` DOMAIN_NAME(apex)

Pass `parent_id` per A1.

## Files

- `modules_v2/_core/topology.py`
- `.seed/scripts/cli_corpus/core/topology.py`
- Unit tests on the helper

## Acceptance

- Unknown vs known name fixtures.
- Copies in sync.
- Index F2 → done.

## Forbidden

Implementing Subfinder/HTTPX/Katana/Pius/Nerva in this issue.

Branch from `develop`; PR into `develop`.
