Spec: SPEC-019 **R19-18**. Parent: Epic F. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

HTTPX hangs `LINKED_URL_INTERNAL` off SCAN_RECORD and puts `HTTP_STATUS_CODE` on SERVICE. Website roots (apex vs subdomain) should own the homepage URL and status.

## Do

Classify probe host: equal to apex → DOMAIN_NAME; suffix-child of apex → SUBDOMAIN (mint under apex if needed). Attach `HTTP_STATUS_CODE` (and existing HTTP_TITLE / tech as today) via `had` on that website root. Homepage probe URL is `LINKED_URL_INTERNAL` **contained by that root**. Off-apex CNAME stays DOMAIN_NAME **without** a new COMPANY. SCAN does not contain internal URLs.

Do **not** switch to catalogue `HTTP_CODE`.

## Files

- `modules_v2/adapters/httpx/hooks.py`
- `modules_v2/_rules/httpx/structure.yaml`

## Acceptance

- Apex probe: DOMAIN_NAME `had` HTTP_STATUS_CODE and `contains` homepage URL.
- `www.apex` probe: SUBDOMAIN owns those facts.
- Index F4 → done.

## Forbidden

HTTP_CODE DATA type; COMPANY per CDN CNAME; SCAN_RECORD-contains every URL.

Branch from `develop`; PR into `develop`.
