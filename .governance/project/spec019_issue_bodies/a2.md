Spec: SPEC-019 **R19-02**. Parent: Epic A. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

`add_host_networks_port_service` calls `nugget_node("TRANSPORT", transport)` with no `parent_id`, so even after A1 a helper that `add_node`s twice without parent still mints extras or fails to reuse intra-host.

## Do

Both `topology.py` copies: pass `parent_id` when adding NETWORKS, TRANSPORT, PORT, SERVICE, HOST, SCAN_RECORD (and related category nodes). Callers of `add_node` in this helper must use the A1 cache API.

## Files

- `modules_v2/_core/topology.py`
- `.seed/scripts/cli_corpus/core/topology.py`
- Adapter call sites only if they bypass the helper and call `nugget_node` + `add_node` without parent (minimal, in-scope of this helper’s users)

## Acceptance

- Two-host synthetic via `add_host_networks_port_service`: each host has its own TRANSPORT/PORT; same host’s second PORT(22) reuses.
- Copies in sync.
- Index A2 → done.

## Forbidden

Rewriting every adapter in this issue (that is F3–F7 / remaining tools). GSE YAML changes (A3).

Branch from `develop`; PR into `develop`.
