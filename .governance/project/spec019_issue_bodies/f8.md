Spec: SPEC-019 **R19-22**. Parent: Epic F. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Need a graph validator so new adapters cannot ship apex DOMAIN_NAME without COMPANY, and so hierarchy tests are synthetic (not old corpus JSON).

## Do

1. Validator: if a graph has a DOMAIN_NAME equal to the scan target/apex, that node must have COMPANY as a `contains` parent. Do **not** require COMPANY on every incidental hostname.
2. Synthetic tests: Subfinder-shaped COMPANY → DOMAIN_NAME → SUBDOMAIN → URL; HTTPX status on website root; Katana pages under matching host.
3. Historical `nugget_structure/` files are **not** this validator’s target.

## Files

- `modules_v2/_core/` or `spiderfeet_v2/` validator module (smallest coherent place)
- Tests next to it

## Acceptance

- Missing COMPANY on apex fails; incidental DOMAIN_NAME without COMPANY passes.
- Synthetic hierarchy tests green.
- Index F8 → done.

## Forbidden

Failing CI on historical corpus graphs; requiring COMPANY on CNAMEs.

Branch from `develop`; PR into `develop`.
