Spec: SPEC-019 **R19-07**. Parent: Epic C. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

`step_runner._build_scan_step_spec` for argv steps passes `{argv, domain/target=first value, timeout}`. Nuclei `_collect_urls` sees 1 URL → `len > batch_size` is false → no batching.

## Do

Pass the full resolved `input_values` as `urls` (or equivalent keys `_collect_urls` already understands) into the Nuclei scan spec. Batch when `len > 20`. 12A stays **one** option pass (empty `option_passes`). Still **one** workflow step.

## Files

- `spiderfeet_v2/engine/step_runner.py`
- `modules_v2/sfp_cli_nuclei.py` (`_collect_urls`, `_run_batched`)

## Acceptance

- 45 URL fixture → 3 batch processes (not 1).
- 1–20 URLs → single process.
- Index C1 → done.

## Forbidden

Per-URL steps; adding CVE/exposure `option_passes` to 12A; changing Composer host widget.

Branch from `develop`; PR into `develop`.
