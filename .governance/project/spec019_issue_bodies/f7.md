Spec: SPEC-019 **R19-21**. Parent: Epic F. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Nerva mints DOMAIN_NAME from HTTP hostnames. Apex hostname needs COMPANY wrap; incidental CDN names do not each get a COMPANY.

## Do

If a DOMAIN_NAME equals the scan apex/hostname, wrap with `add_company_domain_tree`. Off-apex hostnames stay DOMAIN_NAME (or existing type) without a new COMPANY.

## Files

- `modules_v2/adapters/nerva/hooks.py`

## Acceptance

- Apex hostname has COMPANY parent; a CDN hostname in the same graph does not force a second COMPANY.
- Index F7 → done.

## Forbidden

COMPANY per incidental hostname; changing fingerprint mapping beyond the wrap.

Branch from `develop`; PR into `develop`.
