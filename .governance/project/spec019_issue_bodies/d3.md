Spec: SPEC-019 **R19-12**. Parent: Epic D. Plan: `@spiderfeet/.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

SPEC-018 smoke checked `edgeMeta.has(httpx → collector)` (false) and never checked Nice-DAG `collector.dependencies`. Wrong katana collector id / `edgeKey` separator (`|` vs `->`) also hid the bug.

## Do

1. Add `spec019.smoke.mjs` (keep spec018 smoke frozen unless a tiny shared helper is cleaner).
2. Assert `collector.dependencies` for the Nmap rank includes nmap, **not** httpx; Nerva rank includes nerva, **not** katana.
3. Mixed-rank: HTTPX `none` + Nmap export → one collector on CX with Nmap only.
4. Use the real `edgeKey` separator the mapper uses.
5. Addendum on `.governance/specs/SPEC-012-LAYOUT-RULES.md` if needed.

## Files

- `src/workflow-dag/spec019.smoke.mjs` (create)
- SPEC-012 layout rules addendum

## Acceptance

```bash
node src/workflow-dag/spec019.smoke.mjs
```

passes; documents the dependency assertion. Index D3 → done.

## Forbidden

Checking only `edgeMeta`; inventing collector ids that are not `collectorId(stepId)`.

Branch from `develop`; PR into `develop`.
