Spec: SPEC-019 **R19-20**. Parent: Epic F. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Pius roots at `COMPANY_NAME` ENTITY. COMPANY_NAME is now a DESCRIPTOR. Graphs that emit DOMAIN_NAME must have COMPANY as owner.

## Do

Bounded wrap only: COMPANY head; `had` COMPANY_NAME when org string is known; COMPANY `contains` DOMAIN_NAME. No other Pius mapping changes (affiliates, netblocks, pages stay as they are aside from the new head).

## Files

- `modules_v2/adapters/pius/hooks.py`
- Pius structure.yaml / tests that assert COMPANY_NAME as ENTITY root

## Acceptance

- Pius fixture: COMPANY `had` COMPANY_NAME; COMPANY `contains` at least one DOMAIN_NAME.
- Index F6 → done.

## Forbidden

Rewriting Pius classify/plugins; changing netblock/affiliate mapping beyond the head wrap.

Branch from `develop`; PR into `develop`.
