Spec: SPEC-019 **R19-10**. Parent: Epic D. Plan: `@spiderfeet/.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

`mapper.js` still does `const deps = atRank.map((s) => s.id)` then only `edgeMeta.set` for exporters. HTTPX/Katana remain Nice-DAG dependencies → drawn as `followed-by`.

## Do

Collector `dependencies` = exporting steps at that rank (`context.export: scan_graph`) **plus** previous collector. HTTPX/Katana must not appear in `collector.dependencies`. Rank collector still exists if any step on that rank exports. Subfinder export stays.

## Files

- `src/workflow-dag/components/mapper.js` (~line 248)
- Existing smoke only if it already asserts deps; full smoke is D3

## Acceptance

- 12A mapped graph: nmap/nerva in collector deps; httpx/katana absent.
- Index D1 → done.

## Forbidden

Removing the Nmap/Nerva rank collector; hardcoded step-id lists (read YAML `context.export`).

Branch from `develop`; PR into `develop`.
