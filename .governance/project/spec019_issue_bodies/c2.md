Spec: SPEC-019 **R19-08**. Parent: Epic C. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

SPEC-018 locked Composer `0/n` then `n/n` for the whole list. Nuclei batches must report **batch** progress. 12A `timeout: 900` is one wall-clock for the whole step.

## Do

1. While RUNNING, `RunRegistry.set_step_input_progress` with `input_done`/`input_total` = `batches_done`/`batches_total` (existing B2 fields).
2. 12A: `timeout` = per-batch (e.g. 300). Add `overall_timeout` (e.g. 3600) as the step cap. Wire both in the runner/module.
3. 1–20 URLs: `0/1` then `1/1`.

## Files

- `modules_v2/sfp_cli_nuclei.py` (progress_callback)
- `spiderfeet_v2/engine/step_runner.py` / run registry
- `.seed/12A_Workflow_YAML_Example.yaml` (`timeout`, `overall_timeout`)

## Acceptance

- Tests or dry-run show batch counters incrementing.
- Per-batch timeout applies to each chunk; overall cap still kills the step if exceeded.
- Index C2 → done.

## Forbidden

Host-widget copy changes; per-URL fan-out; leaving a single 900s clock shared across batches with no per-batch bound.

Branch from `develop`; PR into `develop`.
