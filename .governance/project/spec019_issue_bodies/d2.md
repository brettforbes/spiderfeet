Spec: SPEC-019 **R19-11**. Parent: Epic D. Plan: `@spiderfeet/.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

`followed-by` / `used-by` must use vertical in/out ports only. `semantic-export` must use horizontal CX. Collapse/expand must not break after D1.

## Do

1. Confirm/fix edge attachment so sequence edges do not land on CX and semantic-export does not land on CY.
2. Outer semantic-export stays inner-context-CX → collector-CX (SPEC-018).
3. **Do not** rewrite Nice-DAG expand internals unless a smoke after D1 still fails.

## Files

- `src/workflow-dag/components/workflowSeedEdgePoints.js`
- `src/workflow-dag/components/edgeMeta.js`
- Layout rules addendum only if D3 owns docs — otherwise a short comment in mapper

## Acceptance

- Sequence edges attach vertically; semantic-export attaches to CX.
- Expand/collapse still lays out 12A without new collector edges on HTTPX/Katana.
- Index D2 → done.

## Forbidden

Full Nice-DAG rewrite; changing Target collector extra-right offset (SPEC-018 C4).

Branch from `develop`; PR into `develop`.
