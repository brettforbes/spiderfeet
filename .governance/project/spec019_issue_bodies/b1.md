Spec: SPEC-019 **R19-05**. Parent: Epic B. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

12A points Nerva at `--output $step.files.output`. Nerva writes JSONL there; stdout is empty. Subfinder/Nmap hydrate from `-o`; Nerva does not.

## Do

After the CLI run, hydrate structured records from `--output` / `-o` the same way Subfinder/Nmap hydrate `-o`. If stdout and the output file are both empty → **ERROR**, not STATUS SUCCESS with an empty graph.

## Files

- `modules_v2/sfp_cli_nerva.py` (or the capture/hydrate path used by the workflow runner)
- Compare: `modules_v2/sfp_cli_subfinder.py` hydrate-from-`-o` (~line 275)

## Acceptance

- Fixture with a non-empty `--output` JSONL file produces a non-empty graph even if stdout is empty.
- Empty file + empty stdout is ERROR.
- Index B1 → done.

## Forbidden

Comma-joined `-t`; treating empty SUCCESS as valid; live scans as the only proof (file fixture is enough).

Branch from `develop`; PR into `develop`.
