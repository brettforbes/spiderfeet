Spec: SPEC-019 **R19-09**. Parent: Epic C. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Katana GSE `crawl_urls` unions URL types **and** `DOMAIN_NAME`, so Nuclei `-l` got hostnames mixed with URLs. Tests must lock batching + skip_step.

## Do

1. 12A `sfp_cli_katana` `crawl_urls`: `nugget_id_in` URL/LINK only (`LINKED_URL_INTERNAL`, `LINKED_URL_EXTERNAL`). Drop `DOMAIN_NAME`.
2. Tests: fake 45 URLs → 3 batches; empty Katana list → Nuclei `skip_step`; 12A still schema-validates.

## Files

- `.seed/12A_Workflow_YAML_Example.yaml`
- `spiderfeet_v2/workflow/tests/`
- Nuclei module tests

## Acceptance

- `crawl_urls` contains no bare hostnames from DOMAIN_NAME.
- Empty input skip_step preserved.
- Index C3 → done.

## Forbidden

Feeding DOMAIN_NAME as fake `http://host/` in this issue (that was a rejected option).

Branch from `develop`; PR into `develop`.
