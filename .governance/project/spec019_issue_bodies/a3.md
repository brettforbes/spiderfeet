Spec: SPEC-019 **R19-03**. Parent: Epic A. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

12A `ip_port_list` uses unbounded `HOST --contains*--> PORT`. Shared TRANSPORT made that cartesian. Nested `for_each` in `gse_eval.py` recurses with `scope_ids=None`, so children are not scoped to the parent even when identity is fixed.

Operator locked: rewrite GSE **anyway** to bind PORT via HOST → NETWORKS → TRANSPORT → PORT (IP via HOST → NETWORKS).

## Do

1. Fix `_eval_for_each` nested path: child match must be restricted to nodes reachable from the parent along the specified relation (honor `reachable_from`).
2. Rewrite `.seed/12A_Workflow_YAML_Example.yaml` `ip_port_list` to that host-scoped path (and 12C if the nested form’s semantics change).
3. Synthetic two-host Nmap-shaped graph (uuid4, A1/A2 builder): `ip:port` count equals real open ports (e.g. 13 not 26). **Do not** use old corpus nmap graphs as the cartesian proof.

## Files

- `spiderfeet_v2/workflow/gse_eval.py`
- `.seed/12A_Workflow_YAML_Example.yaml`
- `.seed/12C_Graph_Select_Language.md` (nested for_each semantics)
- `spiderfeet_v2/workflow/tests/` (synthetic graph tests; skip/xfail corpus cartesian assertions if they still read old uuid5 nmap JSON)

## Acceptance

- Nested for_each is parent-scoped.
- Synthetic two-host: no cross-host `ip:port`.
- Empty collect under a host still emits nothing for that host.
- Index A3 → done.

## Forbidden

TypeQL GSE; corpus re-harvest; treating old nmap corpus JSON as the identity proof.

Branch from `develop`; PR into `develop`.
