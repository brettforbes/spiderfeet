Spec: SPEC-019 **R19-15**. Parent: Epic F. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

No `COMPANY` or `SUBDOMAIN` types. `COMPANY_NAME` is ENTITY (Pius root). SPEC-007 documented that `SUBDOMAIN` does not exist.

## Do

1. Add `COMPANY` and `SUBDOMAIN` (ENTITY) to both `nuggets_extension.json` copies (`.docs/analysis/` and `modules_v2/_catalogues/`).
2. Retype `COMPANY_NAME` to **DESCRIPTOR** in `nuggets.json` and catalogue copies. This is a core type correction (explicit exception to “do not patch nuggets.json for tool-specific additions”).
3. Matching TypeQL in `.seed/spiderfeet_map.tql` and v2 schema if COMPANY/COMPANY_NAME/SUBDOMAIN appear there.
4. Icons: reuse `icon_company_name.svg` for COMPANY; SUBDOMAIN may reuse `icon_domain_name.svg`.

## Files

- `.docs/analysis/nuggets.json`
- `.docs/analysis/nuggets_extension.json`
- `modules_v2/_catalogues/nuggets.json` and `nuggets_extension.json` (keep in sync)
- `.seed/spiderfeet_map.tql` / `.seed/spiderfeet_v2_semantic.tql` as applicable

## Acceptance

- Catalogue loaders resolve the new types and COMPANY_NAME as DESCRIPTOR.
- No adapter behavior change in this issue (F2+).
- Index F1 → done.

## Forbidden

Adapter rewrites; deleting DOMAIN_NAME_PARENT; changing HTTP_STATUS_CODE.

Branch from `develop`; PR into `develop`.
