Spec: SPEC-019 **R19-17**. Parent: Epic F. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Subfinder emits every host as `DOMAIN_NAME` under SCAN; children are distinguished by `DOMAIN_NAME_PARENT` `had`. 12A GSE selects children that way. Seed 09 says no COMPANY without org evidence.

## Do

1. Adapter: seed apex = DOMAIN_NAME under COMPANY (helper). Each child FQDN = SUBDOMAIN contained by the apex (flat). SCAN does not contain SUBDOMAIN.
2. 12A GSE: `apex_domains` = `nugget_id: DOMAIN_NAME`; `subdomains` = `nugget_id: SUBDOMAIN`; `all_domains` = union + `$workflow.inputs.targets`.
3. Update `modules_v2/_rules/subfinder/structure.yaml` and `.seed/09_Ontology_For_Subfinder.md` (overwrite “no COMPANY”).

## Files

- `modules_v2/adapters/subfinder/hooks.py`
- `.seed/scripts/cli_corpus/adapters/subfinder/` if duplicated
- `.seed/12A_Workflow_YAML_Example.yaml`
- `.seed/12B_Workflow_DSL_Description.md` (SUBDOMAIN now exists)
- `_rules/subfinder/structure.yaml`, seed 09

## Acceptance

- Synthetic: COMPANY → DOMAIN_NAME → N SUBDOMAIN nodes; GSE lists split correctly.
- Index F3 → done.

## Forbidden

Nested subdomain trees; deleting DOMAIN_NAME_PARENT from the catalogue; corpus re-harvest.

Branch from `develop`; PR into `develop`.
