Spec: SPEC-019 **R19-01**. Parent: Epic A. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

`nugget_instance_id = {nugget_id}--{uuid5(ONTOLOGY_NAMESPACE, nugget_data)}` plus `validate_graph` forbidding duplicate `(nugget_id, nugget_data)` forces one shared `TRANSPORT(tcp)` across hosts.

## Do

1. Both copies of `graph_builder.py`: ENTITY/SUBENTITY/CATEGORY/INTERNAL mint **uuid4** instance ids; DESCRIPTOR and DATA stay uuid5 unique-by-value.
2. `GraphBuilder.add_node(..., parent_id=)` caches `(parent_id, nugget_id, data)` for uuid4 types. Same parent + same value = reuse; different parents = new uuid4.
3. `validate_graph`: allow duplicate `(nugget_id, data)` for uuid4 types; still error on duplicate ids and on duplicate DESCRIPTOR/DATA pairs.
4. Persist (`spiderfeet_v2/engine/persist.py`) stores graphs as emitted — **no** re-collapse by value on ingest.
5. Unit tests: two HOST parents each with `PORT("22")` → two PORT nodes; two `had` edges to one shared `PORT_STATE("open")` DESCRIPTOR.

## Files

- `modules_v2/_core/graph_builder.py`
- `.seed/scripts/cli_corpus/core/graph_builder.py`
- `modules_v2/_core/tests/` (parity / validate_graph)
- `spiderfeet_v2/engine/persist.py` (read + no-collapse check)

## Acceptance

- Copies stay in sync.
- Synthetic two-parent graph has two TRANSPORT(tcp) nodes.
- DESCRIPTOR unique-by-value still holds.
- Index A1 → done.

## Forbidden

Corpus re-harvest; scoped-uuid5; changing DESCRIPTOR/DATA to uuid4; TypeDB cross-scan merge.

Branch from `develop`; PR into `develop`. Use **this issue’s number** in the branch name.
