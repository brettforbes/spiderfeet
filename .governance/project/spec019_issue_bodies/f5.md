Spec: SPEC-019 **R19-19**. Parent: Epic F. Plan: `.governance/project/SPEC019_AGENT_PLAN.md`.

## Problem

Katana SCAN_RECORD-contains every URL and mints DOMAIN_NAME per host. Internal pages must hang off DOMAIN_NAME or SUBDOMAIN by hostname.

## Do

Hostname match: apex host → DOMAIN_NAME contains URL; child host → matching SUBDOMAIN contains URL (**mint** SUBDOMAIN under apex if HTTPX/Katana sees it first). Off-apex → `LINKED_URL_EXTERNAL`, not hung under the apex. SCAN does not contain internal URLs. COMPANY wrap via helper for the scan apex.

## Files

- `modules_v2/adapters/katana/hooks.py`
- `modules_v2/_rules/katana/structure.yaml`

## Acceptance

- Synthetic crawl: pages under matching website roots; off-apex is EXTERNAL.
- Index F5 → done.

## Forbidden

Attaching unknown hosts to apex; SCAN containing every internal URL; nested SUBDOMAIN.

Branch from `develop`; PR into `develop`.
