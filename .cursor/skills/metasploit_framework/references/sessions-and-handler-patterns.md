# Sessions and Handler Patterns

- Configure `exploit/multi/handler` to match payload (`LHOST`, `LPORT`, payload type).
- Track sessions with `sessions -l`; interact via `sessions -i <id>`.
- Record session metadata as evidence.
- For SpiderFeet alignment, treat session details as sensitive structured findings.
